# === Instale PRIMEIRO o PyTorch CPU-only (senão baixa a versão CUDA): ===
#   uv pip install -i https://download.pytorch.org/whl/cpu torch
#   pip  install -i https://download.pytorch.org/whl/cpu torch
torch>=2.0+cpu

# Kokoro TTS (motor de síntese de fala)
kokoro>=0.9.4,<1

# Pins compatíveis (validados): transformers 5.x exige huggingface_hub v1,
# que emite warning ruidoso de token; mantemos o combo estável:
transformers>=4.50,<5
huggingface_hub>=0.34,<0.35

# Escrita de WAV
soundfile>=0.12.0
